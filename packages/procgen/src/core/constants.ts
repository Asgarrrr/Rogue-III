/**
 * Core Constants
 *
 * Shared numeric constants used across the procgen package.
 */

// =============================================================================
// NUMERIC LIMITS
// =============================================================================

/** Maximum uint32 value for seed generation */
export const MAX_UINT32 = 0xffffffff;

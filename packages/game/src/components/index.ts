/**
 * Game Components
 *
 * Define all ECS components here. Components are pure data.
 *
 * @example
 * ```typescript
 * import { component, i32, f32, str, entityRef } from "@rogue/ecs";
 *
 * @component
 * export class Position {
 *   x = i32(0);
 *   y = i32(0);
 * }
 *
 * @component
 * export class Health {
 *   current = i32(100);
 *   max = i32(100);
 * }
 *
 * @component
 * export class Name {
 *   value = str("Unknown");
 * }
 * ```
 */

export {};
